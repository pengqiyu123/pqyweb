# 阿里云域名DNS管理指南

## 第一步：登录阿里云控制台

1. **访问阿里云官网**：https://www.aliyun.com/
2. **登录账号**：使用你的阿里云账号密码
3. **进入控制台**：点击右上角"控制台"

## 第二步：找到域名管理

在控制台页面：
1. 左侧菜单找到"**域名与网站**" → "**域名**"
2. 或者直接在搜索框搜索"域名"
3. 进入域名列表页面

## 第三步：DNS管理操作

### 查看当前DNS设置
1. 在域名列表中找到你的域名
2. 点击域名进入详情页
3. 找到"**DNS管理**" → "**DNS修改**"
4. 查看当前的DNS服务器地址

### 修改DNS服务器（如使用Cloudflare）

**如果使用Cloudflare Pages，建议修改DNS：**

1. **获取Cloudflare DNS地址**：
   - 登录Cloudflare：https://dash.cloudflare.com/
   - 添加你的域名
   - 获取DNS服务器地址（类似）：
     - `xxx.ns.cloudflare.com`
     - `yyy.ns.cloudflare.com`

2. **在阿里云修改DNS**：
   - 进入域名详情页
   - 点击"DNS修改"
   - 选择"自定义DNS"
   - 输入Cloudflare提供的DNS地址
   - 点击"确认"保存

### 使用阿里云默认DNS（不修改）

如果继续使用阿里云DNS：
1. 进入"**域名解析**"
2. 点击"**添加记录**"
3. 配置DNS记录：
   - **记录类型**：CNAME（用于子域名）或A记录（用于主域名）
   - **主机记录**：@（主域名）或www（www子域名）
   - **记录值**：Cloudflare Pages提供的地址

## 第四步：DNS记录配置示例

### 配置Cloudflare Pages的DNS记录

在阿里云域名解析中：

#### 方案一：使用CNAME记录（推荐）
```
记录类型：CNAME
主机记录：www
记录值：pqy-web-navigation.pages.dev
TTL：10分钟（自动）
```

#### 方案二：使用A记录（主域名）
```
记录类型：A
主机记录：@
记录值：104.21.92.241（Cloudflare IP）
TTL：10分钟（自动）
```

## 第五步：验证DNS生效

DNS修改后需要等待生效：
- **本地生效**：几分钟到几小时
- **全球生效**：24-48小时

### 验证方法：
1. 使用在线DNS查询工具：https://dnschecker.org/
2. 输入你的域名查看DNS解析状态
3. 等待所有地区都显示正确的IP地址

## 常见问题解答

### Q: DNS修改后网站无法访问？
A: 这是正常现象，DNS传播需要时间。请等待24-48小时。

### Q: 如何知道DNS是否生效？
A: 使用`ping 你的域名`命令，查看解析的IP是否正确。

### Q: 修改DNS会影响邮箱吗？
A: 如果域名有邮箱服务，需要确保MX记录正确配置。

### Q: 可以随时改回原来的DNS吗？
A: 是的，DNS修改是可逆的，可以随时改回。

## 注意事项

1. **备份原DNS设置**：修改前记录原来的DNS服务器地址
2. **耐心等待**：DNS传播需要时间，不要频繁修改
3. **检查记录**：确保DNS记录配置正确
4. **测试访问**：在不同网络环境下测试网站访问

## 获取帮助

如果遇到问题：
1. 联系阿里云客服：95187
2. 查看阿里云官方文档
3. 在Cloudflare社区寻求帮助

---

**完成以上步骤后，你的域名就拥有了完整的DNS管理权限，可以配置任何DNS记录来指向你的网站了！**